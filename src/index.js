import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); // 'root' id'li div'e uygulamayı bağlıyoruz
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
